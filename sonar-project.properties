# Unique project key (no spaces, unique in your organization)
sonar.projectKey=MartinBusuulwa256_java-sample

# Human-readable project name
sonar.projectName=java-sample

# Organization key (SonarCloud only; omit for self-hosted)
sonar.organization=martinbusuulwa256

# Source code encoding
sonar.sourceEncoding=UTF-8

# Folder containing your source code
sonar.sources=src

# Exclude tests or irrelevant files
sonar.exclusions=**/test/**
